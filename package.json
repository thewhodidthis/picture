{
  "name": "@thewhodidthis/picture",
  "version": "0.0.0",
  "description": "Helps setup canvas drawings",
  "keywords": [
    "canvas",
    "drawing",
    "picture",
    "sprite"
  ],
  "main": "index.js",
  "homepage": "https://github.com/thewhodidthis/picture#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/thewhodidthis/picture.git"
  },
  "bugs": {
    "url": "https://github.com/thewhodidthis/picture/issues"
  },
  "directories": {
    "example": "example"
  },
  "config": {
    "jscsrc": ".jscsrc"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clear": "npm run clear:dist && npm run clear:logs",
    "clear:dist": "rm -rf dist/",
    "clear:logs": "rm -f *.log",
    "lint": "(jshint index.js & jscs index.js --reporter=inlinesingle) 2>&1 | tee lint-js.log",
    "postlint": "find lint-js.log -size 0 -print0 | xargs -0 rm",
    "start": "npm run watch",
    "watch": "nodemon -e js -w index.js -x 'npm run build'",
    "preexample": "npm run build",
    "example": "npm run example:link && npm run example:serve",
    "example:link": "ln -siv \"$(pwd)/dist/picture.js\" example",
    "example:serve": "php -S localhost:8000 -t example/",
    "prebuild": "mkdir -p dist/",
    "build": "browserify index.js -s Picture -o ./dist/picture.js",
    "build:min": "browserify index.js -s Picture | uglifyjs -mc -o ./dist/picture.min.js",
    "postbuild": "npm run build:min",
    "preversion": "npm run clear && npm run build"
  },
  "engines": {
    "node": ">=4.2.4",
    "npm": ">=3.5.3"
  },
  "dependencies": {},
  "devDependencies": {
    "jscs": "latest",
    "jshint": "latest",
    "uglify-js": "latest",
    "browserify": "latest",
    "nodemon": "latest"
  },
  "author": "sChzs BkgNns <sotirios.bakagiannis@gmail.com> (https://www.thewhodidthis.com)",
  "license": "Unlicense"
}
